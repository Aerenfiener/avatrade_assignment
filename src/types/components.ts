export enum ButtonVisualType {
    DEFAULT = 'default',
    INVISIBLE = 'invisible'
}

export enum ButtonType {
    BUTTON = 'button',
    SUBMIT = 'submit'
}

export enum InputType {
    TEXT = 'text',
    PASSWORD = 'password'
}